
public class Sorter {
	private Strategy = null;
	
	Strategy strat = SortingAlgorithm AlgorithmSelector.select(SequenceType aSequenceType, int aSequenceSize));

	Sorter.setStrategy(strat);
	
	public void setStrategy(Strategy strat){
		this.strategy = strat;
	}

}
